# GAP-RADAR 개발 운영 규칙

이 파일의 규칙은 이 저장소 전체에 적용한다.

## 기본 컨텍스트

- 모든 새로운 작업은 프로젝트 루트의 `PROJECT_CONTEXT.md`를 먼저 끝까지 확인한 뒤 시작한다.
- `PROJECT_CONTEXT.md`의 내용과 현재 코드가 다르면 현재 코드를 사실 기준으로 삼고, 차이를 사용자에게 설명한다.
- 작업으로 구조, 실행법, 환경변수, API, 배포 방식 또는 구현 상태가 달라지면 `PROJECT_CONTEXT.md`도 함께 최신화할지 검토한다.

## 필수 작업 순서

새로운 개발·수정 작업은 다음 순서를 지킨다.

1. `PROJECT_CONTEXT.md` 확인
2. 요청과 관련된 코드 및 설정 파일 확인
3. 기존 Streamlit 앱, FastAPI, 외부 API, Render 배포에 미치는 영향 확인
4. 수정 전에 변경할 파일, 변경 내용, 예상 영향을 짧고 구체적으로 설명
5. 필요한 코드를 직접 수정
6. 영향 범위에 맞는 실행 또는 테스트 수행
7. 발견한 오류가 요청 범위 안에 있으면 수정 후 다시 테스트
8. 최종적으로 변경 파일, 변경 이유, 테스트 결과, 남은 주의사항을 요약

## 사용자 설명 원칙

- 사용자는 개발 초보자라고 가정한다.
- 추상적인 개발 용어만 나열하지 말고 `app.py`, `api/main.py`처럼 실제 파일명과 기능을 기준으로 설명한다.
- 전문 용어가 필요하면 짧은 쉬운 뜻을 함께 설명한다.
- 가능한 경우 사용자가 직접 파일을 편집하도록 시키지 말고 에이전트가 직접 수정한다.
- 사용자가 실행해야 하는 명령은 복사해서 바로 실행할 수 있는 완전한 형태로 제공한다.

## 변경 안전 원칙

- 기존 기능을 임의로 삭제하거나 요청 범위를 넘어 대규모 리팩터링하지 않는다.
- 기존 프로젝트를 깨뜨릴 가능성이 있는 변경이나 폴더·모듈 구조 변경은 수정 전에 이유, 영향, 위험을 먼저 설명한다.
- Streamlit과 FastAPI에 중복된 실거래 처리 규칙을 수정할 때는 양쪽 동작 차이를 확인한다.
- API 응답이나 OpenAPI 설명을 바꿀 때는 ChatGPT Actions와 기존 API 사용자의 호환성을 확인한다.
- `render.yaml`, 환경변수 이름, 실행 명령을 바꿀 때는 로컬 및 Render 배포 영향을 확인한다.
- API Key, 비밀번호, 토큰 등 Secret을 코드, 문서, 로그 또는 Git에 넣지 않는다.

## Git 운영 원칙

- 작업 시작 전과 종료 후 `git status`를 확인한다.
- 사용자의 기존 변경사항을 덮어쓰거나 되돌리지 않는다.
- 사용자가 명시적으로 요청하지 않으면 `git push`하지 않는다.
- 커밋도 사용자가 요청하거나 승인한 경우에만 수행한다.
- 커밋이 적절한 작업이라면 변경 내용에 맞는 commit message를 함께 제안한다.
- 최종 보고 시 커밋되지 않은 파일과 테스트 결과를 명확히 알려준다.

## 테스트 원칙

- 최소한 변경한 Python 파일의 구문 검사를 수행한다.
- Streamlit 변경은 가능한 범위에서 검색, 필터, 리스트, 지도, 엑셀 중 영향을 받는 기능을 확인한다.
- FastAPI 변경은 가능한 범위에서 앱 로딩, `/health`, `/docs` 또는 `/openapi.json`, 관련 엔드포인트와 인증 오류를 확인한다.
- 외부 API를 실제 호출하지 못했으면 테스트를 통과했다고 표현하지 말고, 수행하지 못한 이유와 사용자가 확인할 항목을 분리해 적는다.
- 테스트 실패를 숨기지 않고 원인, 수정 여부, 남은 제한을 설명한다.

